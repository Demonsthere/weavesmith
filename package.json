{
  "name": "weavesmith",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "prepare": "pnpm -r build"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "2.1.9"
  }
}
